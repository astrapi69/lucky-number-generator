<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.alpharogroup.android.lucky_number_generator">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/my_backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ImportLotteryNumberCountListActivity" />
        <activity android:name=".LotteryNumberCountListActivity" />
        <activity android:name=".CustomGenerationActivity" />
        <activity android:name=".EurojackpotGenerationActivity" />
        <activity android:name=".Lottery6Of49GenerationActivity" />
        <activity android:name=".DrawingActivity" />
        <activity android:name=".GenerationResultActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>